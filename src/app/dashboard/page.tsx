import { SignOutButton } from '@clerk/nextjs'
import React from 'react'

const page = () => {
  return (
    <div>
        <SignOutButton>ndnd</SignOutButton>
    
    </div>
  )
}

export default page
